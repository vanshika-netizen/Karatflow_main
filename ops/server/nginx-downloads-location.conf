# Add this location block inside the existing HTTPS server block.
location /downloads/ {
    alias /var/www/app-downloads/;
    index index.html;
    types {
        application/vnd.android.package-archive apk;
    }
    add_header Cache-Control "no-cache" always;
}
